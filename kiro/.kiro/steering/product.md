# Product

A simple browser-based ToDo application. Users can manage their daily tasks through a clean, minimal UI served by a lightweight Python backend.

## Key Features
- Add new tasks
- Edit/update existing tasks
- Delete tasks
- Set reminders for tasks
- Mark tasks as complete

## Target Users
Individual users who want a lightweight, self-hosted task manager accessible via a web browser.
